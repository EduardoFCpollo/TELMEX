function editarCliente(id) {
  console.log("Editar cliente:", id);
  // Aquí llamas el modal o formulario de edición
  // Ejemplo:
  const modal = new bootstrap.Modal(document.getElementById('modalEditarCliente'));
  modal.show();
}
